var seconds = 20;
var minutes = 20;
var hour = 1;

function displayTimer()
{
    alert(hour + ":" + minutes + ":" + seconds);
}

//window.document.getElementById("lblSeconds").innerHTML = second.toString(); // Syntax error caught at compile time

//window.onload = displayTimer(true); // Syntax error caught at compile time

